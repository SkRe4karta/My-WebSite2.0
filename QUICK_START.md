# 🚀 Быстрый старт

## Настройка для входа с учетными данными:
- **Логин:** `skre4karta`
- **Пароль:** `1234`

## Шаги настройки:

### 1. Создайте .env файл

```powershell
npm run setup:env
```

Или создайте файл `.env` вручную в корне проекта:

```env
DATABASE_URL="postgresql://mywebsite:changeme@localhost:5432/mywebsite?schema=public"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="ваш-секретный-ключ"
ADMIN_EMAIL="zelyonkin.d@gmail.com"
ADMIN_USERNAME="skre4karta"
ADMIN_PASSWORD="1234"
NODE_ENV="development"
```

### 2. Запустите PostgreSQL

**Вариант A: Docker Compose (рекомендуется)**
```powershell
docker-compose up -d postgres
```

**Вариант B: Локальный PostgreSQL**
Убедитесь, что PostgreSQL установлен и запущен на `localhost:5432`

### 3. Инициализируйте базу данных

```powershell
npm run db:init
```

Это создаст все необходимые таблицы в базе данных.

### 4. Создайте администратора

```powershell
npm run db:seed
```

Это создаст пользователя с логином `skre4karta` и паролем `1234`.

### 5. Добавьте проект о сайте (опционально)

```powershell
npm run db:add-website-project
```

Это добавит проект "My-WebSite 2.0" в портфолио.

### 6. Запустите dev-сервер

```powershell
npm run dev
```

Откройте браузер: http://localhost:3000

## Вход в систему

1. Перейдите на http://localhost:3000/login
2. Введите:
   - **Логин:** `skre4karta`
   - **Пароль:** `1234`

## Устранение проблем

### База данных недоступна

Если видите ошибку `Can't reach database server at localhost:5432`:

1. Проверьте, что PostgreSQL запущен:
   ```powershell
   docker-compose ps
   ```

2. Если используете Docker, запустите:
   ```powershell
   docker-compose up -d postgres
   ```

3. Проверьте DATABASE_URL в `.env` файле

### Ошибка аутентификации

Если не можете войти:

1. Убедитесь, что администратор создан:
   ```powershell
   npm run db:seed
   ```

2. Проверьте, что в `.env` указаны правильные:
   - `ADMIN_USERNAME=skre4karta`
   - `ADMIN_PASSWORD=1234`

3. Перезапустите dev-сервер после изменения `.env`

## Полезные команды

- `npm run db:studio` - Открыть Prisma Studio для просмотра БД
- `npm run db:push` - Применить изменения схемы без миграций
- `npm run lint` - Проверить код на ошибки

