# 🔍 Диагностика проблем с Healthcheck

## Проблема: Контейнер не проходит healthcheck

Если контейнер `portfolio_web` не запускается с ошибкой "unhealthy", выполните следующие шаги:

## 1. Проверка логов контейнера

```bash
cd /var/www/zelyonkin.ru

# Просмотр логов
docker-compose logs web

# Просмотр последних 50 строк
docker-compose logs --tail=50 web

# Просмотр логов в реальном времени
docker-compose logs -f web
```

## 2. Проверка статуса контейнера

```bash
# Статус всех контейнеров
docker-compose ps

# Детальная информация о контейнере
docker inspect portfolio_web | grep -A 10 Health
```

## 3. Ручная проверка healthcheck

```bash
# Попытка подключиться к API внутри контейнера
docker-compose exec web node -e "require('http').get('http://localhost:3000/api/health', (r) => {r.on('data', (d) => console.log(d.toString())); process.exit(r.statusCode === 200 ? 0 : 1)})"

# Или через curl (если установлен)
docker-compose exec web curl http://localhost:3000/api/health
```

## 4. Проверка базы данных

```bash
# Проверка наличия базы данных
ls -la database/

# Проверка прав на базу данных
ls -l database/db.sqlite

# Выполнение миграций вручную
docker-compose exec web npm run db:migrate
```

## 5. Проверка переменных окружения

```bash
# Проверка .env файла
cat .env | grep -v SECRET

# Проверка переменных в контейнере
docker-compose exec web env | grep -E "DATABASE_URL|NODE_ENV|NEXTAUTH"
```

## 6. Временное отключение healthcheck

Если нужно запустить контейнер без healthcheck для диагностики:

```bash
# Остановите контейнеры
docker-compose down

# Временно закомментируйте healthcheck в docker-compose.yml
# Затем запустите снова
docker-compose up -d web

# Проверьте логи
docker-compose logs -f web
```

## 7. Увеличение времени ожидания

Если приложение долго запускается (миграции БД, генерация Prisma Client), увеличьте `start_period` в `docker-compose.yml`:

```yaml
healthcheck:
  start_period: 180s  # Увеличьте до 3 минут
  retries: 5          # Увеличьте количество попыток
```

## 8. Проверка порта

```bash
# Проверка, что приложение слушает порт 3000
docker-compose exec web netstat -tlnp | grep 3000

# Или через ss
docker-compose exec web ss -tlnp | grep 3000
```

## 9. Пересборка образа

Если проблема сохраняется, пересоберите образ:

```bash
# Остановите контейнеры
docker-compose down

# Удалите старый образ
docker rmi zelyonkinru-web:latest

# Пересоберите
docker-compose build --no-cache web

# Запустите снова
docker-compose up -d web
```

## 10. Проверка дискового пространства

```bash
# Проверка свободного места
df -h

# Очистка неиспользуемых образов и контейнеров
docker system prune -a
```

## Типичные проблемы и решения

### Проблема: База данных не создана
**Решение:**
```bash
docker-compose exec web npm run db:migrate
docker-compose exec web npm run db:init-admin
```

### Проблема: Prisma Client не сгенерирован
**Решение:**
```bash
docker-compose exec web npm run db:generate
```

### Проблема: Недостаточно памяти
**Решение:**
- Увеличьте swap или память сервера
- Ограничьте ресурсы контейнера в `docker-compose.yml`

### Проблема: Порт 3000 занят
**Решение:**
```bash
# Проверка, что порт свободен
netstat -tlnp | grep 3000

# Если занят, остановите процесс или измените порт
```

## Полная переустановка

Если ничего не помогает:

```bash
cd /var/www/zelyonkin.ru

# Остановите и удалите все
docker-compose down -v

# Удалите образы
docker rmi zelyonkinru-web:latest

# Очистите базу данных (ОСТОРОЖНО: удалит все данные!)
rm -rf database/*

# Запустите установку заново
./install.sh
```

---

**Версия:** 2.2.1  
**Дата:** 2025-01-18

