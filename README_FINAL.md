# My-WebSite 2.0 - Финальная версия для развертывания

## ✅ Что было исправлено

1. **Исправлен скрипт добавления проекта** - теперь загружает .env файл
2. **Добавлена информация о проекте в портфолио** - описание My-WebSite 2.0 и указание, что следующие проекты в разработке
3. **Исправлена проблема с билдом** - улучшена конфигурация Next.js для корректной обработки CSS в production
4. **Улучшена обработка ошибок API** - возвращает пустой массив вместо ошибки при недоступности БД
5. **Создана полная инструкция для развертывания на VM**

## 🚀 Быстрый старт для разработки

```bash
# 1. Установите зависимости
npm install

# 2. Создайте .env файл
npm run setup:env
# Или создайте вручную на основе env.example

# 3. Запустите PostgreSQL (Docker)
docker-compose up -d postgres

# 4. Инициализируйте БД
npm run db:init

# 5. Создайте администратора
npm run db:seed

# 6. Добавьте проект о сайте
npm run db:add-website-project

# 7. Запустите dev-сервер
npm run dev
```

**Данные для входа:**
- Логин: `skre4karta`
- Пароль: `1234`

## 📦 Сборка для production

```bash
# 1. Убедитесь, что .env настроен правильно
cat .env

# 2. Соберите проект
npm run build

# 3. Проверьте, что сборка успешна
ls -la .next/standalone

# 4. Запустите production сервер
npm start
```

## 🌐 Развертывание на VM

Полная инструкция находится в файле **DEPLOY_VM.md**

Основные шаги:
1. Установите Node.js, PostgreSQL, Nginx
2. Настройте .env файл
3. Инициализируйте БД
4. Соберите проект
5. Настройте systemd service
6. Настройте Nginx как reverse proxy
7. Настройте SSL (Let's Encrypt)

## 📝 Важные файлы

- `DEPLOY_VM.md` - Полная инструкция для развертывания на VM
- `QUICK_START.md` - Быстрый старт для разработки
- `env.example` - Пример .env файла
- `.env` - Ваш конфигурационный файл (создайте на основе env.example)

## 🔧 Основные команды

```bash
# Разработка
npm run dev              # Запуск dev-сервера
npm run build            # Сборка для production
npm start                # Запуск production сервера

# База данных
npm run db:init          # Инициализация БД
npm run db:seed           # Создание администратора
npm run db:add-website-project  # Добавление проекта о сайте
npm run db:studio         # Открыть Prisma Studio
npm run migrate           # Применить миграции

# Утилиты
npm run setup:env         # Создать .env файл (Windows)
npm run lint              # Проверить код
```

## 🐛 Устранение проблем

### Белый экран / нет стилей после билда

1. Убедитесь, что сборка прошла успешно:
   ```bash
   npm run build
   ls -la .next/standalone
   ```

2. Проверьте, что все зависимости установлены:
   ```bash
   npm install
   ```

3. Убедитесь, что .env файл настроен правильно

4. Проверьте логи:
   ```bash
   # В dev режиме
   npm run dev
   
   # В production
   npm start
   ```

### Ошибки подключения к БД

1. Проверьте, что PostgreSQL запущен:
   ```bash
   docker-compose ps
   # или
   sudo systemctl status postgresql
   ```

2. Проверьте DATABASE_URL в .env

3. Проверьте права пользователя БД

### Ошибка "Environment variable not found: DATABASE_URL"

1. Убедитесь, что .env файл существует в корне проекта
2. Проверьте, что DATABASE_URL указан в .env
3. Перезапустите dev-сервер после изменения .env

## 📋 Структура проекта

```
My-WebSite2.0/
├── src/
│   ├── app/              # Next.js App Router
│   │   ├── admin/        # Админ-панель
│   │   ├── api/          # API routes
│   │   └── page.tsx      # Главная страница
│   ├── components/       # React компоненты
│   │   ├── admin/        # Компоненты админки
│   │   └── public/       # Публичные компоненты
│   ├── lib/              # Утилиты и библиотеки
│   └── styles/           # CSS стили
├── prisma/               # Prisma схема
├── scripts/              # Скрипты для инициализации
├── .env                  # Конфигурация (создайте)
├── next.config.js        # Конфигурация Next.js
└── package.json          # Зависимости
```

## 🔐 Безопасность

- Используйте сильные пароли для БД и админки
- Настройте SSL (HTTPS) для production
- Регулярно обновляйте зависимости: `npm audit fix`
- Делайте резервные копии БД регулярно

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `npm run dev` или `npm start`
2. Проверьте .env файл
3. Убедитесь, что все зависимости установлены
4. Проверьте инструкции в DEPLOY_VM.md

## 🎯 Следующие шаги после развертывания

1. Настройте резервное копирование БД
2. Настройте мониторинг (логи, метрики)
3. Настройте fail2ban для защиты от брутфорса
4. Регулярно обновляйте систему и зависимости

---

**Версия:** 2.0.0  
**Последнее обновление:** 2024

