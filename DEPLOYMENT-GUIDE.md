# 📘 Полное руководство по развертыванию версии 2.2.1

## 📋 Содержание

1. [Подготовка к развертыванию](#подготовка-к-развертыванию)
2. [Коммит и отправка в git](#коммит-и-отправка-в-git)
3. [Обновление на ВМ](#обновление-на-вм)
4. [Проверка работы](#проверка-работы)
5. [Решение проблем](#решение-проблем)

---

## 🎯 Подготовка к развертыванию

### Проверка изменений

```bash
cd my-portfolio-site
git status
```

**Ожидаемые изменения:**
- ✅ Новые файлы: `scripts/`, `src/app/api/users/`, `src/components/admin/UserManagement.tsx`
- ✅ Измененные: `package.json`, `src/lib/auth.ts`, `Dockerfile`, `install.sh`, `deploy.sh`
- ✅ Документация: `CHANGELOG.md`, `DEPLOY-UPDATE.md`, `DIAGNOSE-LOGIN.md`

### Проверка .gitignore

Убедитесь, что следующие файлы НЕ попадут в git:
- ❌ `.env` - переменные окружения
- ❌ `database/*.sqlite` - база данных
- ❌ `storage/uploads/*` - загруженные файлы
- ❌ `node_modules/` - зависимости

---

## 📤 Коммит и отправка в git

### Шаг 1: Добавление файлов

```bash
# Добавить все изменения
git add .

# Или выборочно:
git add scripts/
git add src/app/api/users/
git add src/components/admin/
git add src/lib/auth.ts src/lib/db.ts
git add package.json Dockerfile install.sh deploy.sh
git add *.md
```

### Шаг 2: Создание коммита

```bash
git commit -m "feat: управление пользователями и исправление аутентификации (v2.2.1)

Основные изменения:
- Исправлена аутентификация NextAuth (поддержка множественных пользователей)
- Добавлено управление пользователями в настройках админки
- Исправлены миграции базы данных (prisma migrate deploy)
- Добавлены диагностические скрипты для отладки
- Автоматическое создание администратора при установке
- Улучшена конфигурация Nginx для Next.js
- Исправлена инициализация пользователя (name = skre4karta)

Новые возможности:
- CRUD операции для пользователей через API (/api/users)
- UI компонент UserManagement в настройках
- 5 диагностических npm скриптов (db:test-login, db:check-auth, etc.)
- Автоматическое исправление пользователей

Исправления:
- Убрана привязка к ADMIN_USERNAME в аутентификации
- Убран PrismaAdapter при JWT strategy (исправлен конфликт)
- Исправлена работа сессий (получение данных из БД)
- Улучшена обработка ошибок миграций
- Автоматическая миграция plain text паролей в bcrypt"
```

### Шаг 3: Отправка в репозиторий

```bash
git push origin main
```

---

## 🚀 Обновление на ВМ

### Шаг 1: Подключение к серверу

```bash
ssh admin@82.202.138.157
```

### Шаг 2: Резервная копия (рекомендуется)

```bash
cd /var/www/zelyonkin.ru

# Создайте бэкап
./backup.sh

# Или вручную
cp .env .env.backup
cp -r database database.backup
```

### Шаг 3: Обновление кода

```bash
# Получите обновления
git pull origin main

# Проверьте изменения
git log --oneline -1
```

### Шаг 4: Остановка контейнеров

```bash
docker-compose down
```

### Шаг 5: Пересборка образов

```bash
# Полная пересборка (рекомендуется)
docker-compose build --no-cache

# Или быстрая (если нет изменений в зависимостях)
# docker-compose build
```

### Шаг 6: Запуск контейнеров

```bash
docker-compose up -d

# Дождитесь запуска (15-30 секунд)
sleep 20
```

### Шаг 7: Применение миграций

```bash
# Примените миграции базы данных
docker-compose exec web npm run db:migrate
```

### Шаг 8: Исправление пользователя

```bash
# Исправьте пользователя (обновит name и пароль)
docker-compose exec web npm run db:force-fix-user
```

### Шаг 9: Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs web | tail -30
```

---

## ✅ Проверка работы

### 1. Проверка контейнеров

```bash
docker-compose ps
```

**Ожидаемый результат:**
```
NAME                STATUS
portfolio_web       Up (healthy)
portfolio_nginx     Up (healthy)
```

### 2. Проверка healthcheck

```bash
curl http://localhost/api/health
```

**Ожидаемый результат:** `{"status":"ok"}`

### 3. Проверка настроек аутентификации

```bash
docker-compose exec web npm run db:check-auth
```

**Ожидаемый результат:**
- ✅ Все переменные окружения заданы
- ✅ Администратор найден в БД
- ✅ Пароль хеширован (bcrypt)

### 4. Тестирование входа

```bash
docker-compose exec web npm run db:test-login
```

**Ожидаемый результат:**
- ✅ Пользователь найден
- ✅ Пароль валиден
- ✅ ВХОД ДОЛЖЕН РАБОТАТЬ

### 5. Проверка в браузере

1. Откройте: `http://82.202.138.157` или `http://zelyonkin.ru`
2. Перейдите на `/login`
3. Войдите с:
   - **Логин:** `skre4karta` или `zelyonkin.d@gmail.com`
   - **Пароль:** `1234`
4. Проверьте работу управления пользователями в настройках

---

## 🔧 Решение проблем

### Проблема: Не работает вход

```bash
# 1. Исправьте пользователя
docker-compose exec web npm run db:force-fix-user

# 2. Проверьте результат
docker-compose exec web npm run db:test-login

# 3. Проверьте логи при попытке входа
docker-compose logs -f web
```

**Подробнее:** См. `DIAGNOSE-LOGIN.md`

### Проблема: Ошибки миграций

```bash
# Проверьте базу данных
docker-compose exec web npx prisma studio
# Откройте http://localhost:5555

# Или примените миграции вручную
docker-compose exec web npx prisma migrate deploy
```

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs web
docker-compose logs nginx

# Проверьте конфигурацию
docker-compose config

# Перезапустите
docker-compose restart
```

### Проблема: Nginx не работает

```bash
# Проверьте конфигурацию Nginx
docker-compose exec nginx nginx -t

# Проверьте логи
docker-compose logs nginx

# Перезапустите
docker-compose restart nginx
```

---

## 📝 Чеклист развертывания

- [ ] Код обновлен на локальной машине
- [ ] Все изменения закоммичены
- [ ] Изменения отправлены в git (`git push`)
- [ ] На ВМ выполнен `git pull`
- [ ] Создана резервная копия
- [ ] Контейнеры остановлены
- [ ] Образы пересобраны
- [ ] Контейнеры запущены
- [ ] Миграции применены
- [ ] Пользователь исправлен
- [ ] Проверен статус контейнеров
- [ ] Проверен healthcheck
- [ ] Проверен вход в админку
- [ ] Проверено управление пользователями
- [ ] Сайт доступен и работает

---

## 📚 Дополнительная документация

- **`DEPLOY-UPDATE.md`** - подробная инструкция по обновлению
- **`DIAGNOSE-LOGIN.md`** - диагностика проблем с входом
- **`GIT-COMMIT.md`** - команды для git
- **`QUICK-DEPLOY-COMMANDS.md`** - быстрые команды
- **`UPDATE-SUMMARY.md`** - краткая сводка изменений
- **`CHANGELOG.md`** - история всех изменений

---

## 🎯 Быстрая команда (все в одном)

```bash
# На ВМ - полное обновление одной командой
cd /var/www/zelyonkin.ru && \
git pull origin main && \
docker-compose down && \
docker-compose build --no-cache && \
docker-compose up -d && \
sleep 20 && \
docker-compose exec web npm run db:migrate && \
docker-compose exec web npm run db:force-fix-user && \
docker-compose exec web npm run db:test-login && \
echo "✅ Обновление завершено успешно!"
```

---

**Версия:** 2.2.1  
**Дата:** 2025-01-18  
**Статус:** Готово к развертыванию ✅

