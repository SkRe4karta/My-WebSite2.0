# 🔧 Исправление проблемы с базой данных

## Проблема: "Error code 14: Unable to open the database file"

Эта ошибка означает, что база данных SQLite не может быть открыта. Возможные причины:
- Файл базы данных не существует
- Нет прав на запись в директорию `database/`
- Директория `database/` не существует

## ✅ Решение

### Шаг 1: Проверка директории и прав

```bash
cd /var/www/zelyonkin.ru

# Проверка существования директории
ls -la database/

# Если директории нет, создайте её
mkdir -p database
chmod 755 database

# Проверка прав
ls -ld database/
```

### Шаг 2: Выполнение миграций

```bash
# Убедитесь, что контейнер web запущен
docker-compose ps web

# Если не запущен, запустите его
docker-compose up -d web

# Подождите 10-15 секунд
sleep 15

# Выполните миграции
docker-compose exec web npm run db:migrate
```

### Шаг 3: Проверка создания базы данных

```bash
# Проверка наличия файла базы данных
ls -la database/db.sqlite

# Если файл создан, установите правильные права
chmod 666 database/db.sqlite
chmod 755 database
```

### Шаг 4: Создание администратора

```bash
# Создание/исправление администратора
docker-compose exec web npm run db:force-fix-user
```

### Шаг 5: Проверка

```bash
# Проверка настроек
docker-compose exec web npm run db:check-auth

# Тест входа
docker-compose exec web npm run db:test-login
```

## 🔄 Полное исправление (если ничего не помогло)

```bash
cd /var/www/zelyonkin.ru

# Остановите контейнеры
docker-compose down

# Удалите старую базу данных (ОСТОРОЖНО: удалит все данные!)
rm -rf database/*

# Убедитесь, что директория существует
mkdir -p database
chmod 755 database

# Запустите только web контейнер
docker-compose up -d web

# Подождите 15 секунд
sleep 15

# Выполните миграции
docker-compose exec web npm run db:migrate

# Проверьте создание базы данных
ls -la database/db.sqlite

# Если файл создан, установите права
chmod 666 database/db.sqlite

# Создайте администратора
docker-compose exec web npm run db:force-fix-user

# Запустите nginx
docker-compose up -d nginx

# Проверьте статус
docker-compose ps
```

## 🔍 Диагностика

### Проверка логов

```bash
# Логи контейнера web
docker-compose logs web --tail=50

# Поиск ошибок базы данных
docker-compose logs web | grep -i "database\|sqlite\|error"
```

### Проверка переменных окружения

```bash
# Проверка DATABASE_URL в контейнере
docker-compose exec web env | grep DATABASE_URL

# Должно быть: DATABASE_URL=file:./database/db.sqlite
```

### Проверка прав в контейнере

```bash
# Проверка прав на директорию database внутри контейнера
docker-compose exec web ls -la /app/database/

# Проверка прав на файл базы данных
docker-compose exec web ls -l /app/database/db.sqlite
```

## ⚠️ Важные моменты

1. **Права на директорию:** Директория `database/` должна иметь права `755` или `777`
2. **Права на файл:** Файл `db.sqlite` должен иметь права `666` или `777`
3. **Владелец:** Убедитесь, что файлы доступны пользователю внутри контейнера (обычно `nextjs` с UID 1001)
4. **Путь:** Убедитесь, что `DATABASE_URL` указывает на правильный путь: `file:./database/db.sqlite`

## 📝 После исправления

После успешного создания базы данных:

1. Проверьте вход в админку
2. Смените пароль по умолчанию
3. Проверьте работу всех функций

---

**Версия:** 2.2.1  
**Дата:** 2025-01-18

