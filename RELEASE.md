# Инструкция по развертыванию релизной версии 2.2.0

Полная инструкция по развертыванию портфолио сайта zelyonkin.ru на виртуальной машине Ubuntu 22.04.

## 📋 Содержание

1. [Подготовка проекта](#подготовка-проекта)
2. [Копирование на сервер](#копирование-на-сервер)
3. [Первоначальная настройка сервера](#первоначальная-настройка-сервера)
4. [Автоматическая настройка проекта](#автоматическая-настройка-проекта)
5. [Настройка SSL сертификата](#настройка-ssl-сертификата)
6. [Первый вход и смена пароля](#первый-вход-и-смена-пароля)
7. [Управление приложением](#управление-приложением)
8. [Резервное копирование](#резервное-копирование)
9. [Решение проблем](#решение-проблем)

---

## Подготовка проекта

### Структура для деплоя

Убедитесь, что в папке проекта есть следующие файлы и директории:

```
my-site/
├── install.sh                # ⭐ ЕДИНЫЙ СКРИПТ УСТАНОВКИ (ГЛАВНЫЙ)
├── server-setup.sh           # Скрипт настройки сервера (один раз)
├── setup.sh                  # Перенаправляет на install.sh
├── setup-env.sh              # Скрипт генерации .env
├── deploy.sh                 # Скрипт развертывания (используется install.sh)
├── setup-ssl.sh              # Скрипт получения SSL сертификата
├── backup.sh                 # Скрипт резервного копирования
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile                # Docker образ
├── nginx.conf                # Конфигурация Nginx
├── package.json              # Зависимости проекта
├── prisma/                   # Prisma ORM и миграции
├── src/                      # Исходный код приложения
├── public/                   # Статические файлы
└── README-DEPLOY.md          # Краткая инструкция по развертыванию
```

**Важно:** Все скрипты должны быть исполняемыми. На Linux это будет сделано автоматически, но при копировании через SCP убедитесь, что права сохранены.

---

## Копирование на сервер

### Шаг 1: Создание директории на сервере

```bash
# Подключитесь к серверу
ssh admin@82.202.138.157

# Создайте директорию для проекта
mkdir -p ~/var/www
exit
```

### Шаг 2: Копирование проекта

**С локального компьютера (Windows PowerShell):**

```powershell
# Перейдите в папку с проектом
cd C:\Users\zelyo\.vscode\my-portfolio-site

# Скопируйте весь проект на сервер
scp -r * admin@82.202.138.157:~/var/www/my-site/

# Или если копируете всю папку:
scp -r my-portfolio-site admin@82.202.138.157:~/var/www/
```

**На сервере переименуйте папку (если нужно):**

```bash
ssh admin@82.202.138.157
cd ~/var/www
mv my-portfolio-site my-site
cd my-site
```

**Проверьте, что все файлы скопированы:**

```bash
ls -la
# Должны быть видны все скрипты: server-setup.sh, setup.sh, deploy.sh и т.д.
```

---

## Первоначальная настройка сервера

**⚠️ ВАЖНО:** Этот шаг нужно выполнить только один раз при первой настройке сервера.

### Шаг 1: Запуск скрипта настройки сервера

```bash
# На сервере
cd ~/var/www/my-site

# Установите права на выполнение
chmod +x *.sh

# Запустите скрипт настройки сервера
./server-setup.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит необходимые пакеты (curl, git, openssl, ufw)
- ✅ Установит Docker и Docker Compose
- ✅ Настроит файрвол (откроет порты 22, 80, 443)
- ✅ Создаст директорию `~/var/www`

### Шаг 2: Повторный вход (если Docker был установлен)

Если Docker был только что установлен, необходимо выйти и войти снова:

```bash
exit
ssh admin@82.202.138.157
cd ~/var/www/my-site
```

**Проверьте установку Docker:**

```bash
docker --version
docker-compose --version
```

Оба должны показать версии. Если нет — повторите вход.

---

## Автоматическая настройка проекта

### Запуск единого скрипта установки

```bash
cd ~/var/www/my-site

# Установите права на скрипты
chmod +x *.sh

# Запустите единый скрипт установки
./install.sh
```

**Скрипт `install.sh` автоматически выполнит:**

1. ✅ **Проверку окружения** — убедится, что Docker установлен
2. ✅ **Создание .env файла** — автоматически сгенерирует `.env` с паролем по умолчанию "1234"
3. ✅ **Создание директорий** — создаст все необходимые папки (database, storage, certbot)
4. ✅ **Установку прав** — сделает все скрипты исполняемыми
5. ✅ **Остановку старых контейнеров** — очистит предыдущие запуски
6. ✅ **Сборку и запуск** — соберет и запустит Docker контейнеры
7. ✅ **Миграции БД** — выполнит миграции базы данных

**Время выполнения:** 5-15 минут (зависит от скорости интернета и сервера)

**Альтернатива:** Можно также использовать `./setup.sh` (он перенаправляет на `install.sh`)

### Что происходит при развертывании

1. **Сборка Docker образов** — создание образа Next.js приложения
2. **Запуск контейнеров:**
   - `portfolio_web` — веб-приложение (порт 3000 внутри Docker)
   - `portfolio_nginx` — Nginx reverse proxy (порты 80 и 443)
   - `portfolio_certbot` — автоматическое обновление SSL сертификатов

3. **Выполнение миграций** — создание структуры базы данных

### Проверка статуса

После завершения скрипта проверьте статус контейнеров:

```bash
docker-compose ps
```

Все контейнеры должны быть в статусе "Up". Если какой-то контейнер не запустился, проверьте логи:

```bash
docker-compose logs [имя_контейнера]
# Например: docker-compose logs web
```

---

## Настройка SSL сертификата

**⚠️ ВАЖНО:** Перед получением SSL сертификата убедитесь, что:
- Домен `zelyonkin.ru` указывает на IP адрес вашего сервера (82.202.138.157)
- Порты 80 и 443 открыты в файрволе
- Контейнеры запущены и работают

### Получение SSL сертификата

```bash
cd ~/var/www/my-site

# Запустите скрипт получения SSL сертификата
./setup-ssl.sh
```

**Скрипт автоматически:**
1. ✅ Остановит Nginx временно
2. ✅ Получит SSL сертификат от Let's Encrypt
3. ✅ Запустит Nginx обратно
4. ✅ Применит сертификат

**Время выполнения:** 1-2 минуты

### Проверка SSL сертификата

После успешного получения сертификата проверьте:

```bash
# Проверьте наличие файлов сертификата
ls -la certbot/live/zelyonkin.ru/

# Должны быть файлы:
# - fullchain.pem
# - privkey.pem
```

### Автоматическое обновление

SSL сертификат будет автоматически обновляться каждые 12 часов через контейнер `portfolio_certbot`. Никаких дополнительных действий не требуется.

---

## Первый вход и смена пароля

### Шаг 1: Откройте сайт в браузере

Перейдите на: **https://zelyonkin.ru/login**

### Шаг 2: Вход в админку

**Учетные данные по умолчанию:**
- **Логин:** `skre4karta`
- **Пароль:** `1234`

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Не используйте пароль по умолчанию в продакшене!

### Шаг 3: Смена пароля

1. После входа перейдите в **Настройки** (иконка шестеренки в боковом меню)
2. Найдите раздел **"Смена пароля"**
3. Введите:
   - Текущий пароль: `1234`
   - Новый пароль: (придумайте надежный пароль)
   - Подтверждение пароля: (повторите новый пароль)
4. Нажмите **"Изменить пароль"**

**Рекомендации по паролю:**
- Минимум 8 символов
- Используйте буквы, цифры и специальные символы
- Не используйте простые слова или даты
- Сохраните пароль в безопасном месте (менеджер паролей)

### Шаг 4: Настройка 2FA (опционально, но рекомендуется)

1. В разделе **Настройки** найдите **"Двухфакторная аутентификация"**
2. Нажмите **"Включить 2FA"**
3. Отсканируйте QR-код в приложении-аутентификаторе (Google Authenticator, Authy и т.д.)
4. Введите код подтверждения
5. Сохраните резервные коды в безопасном месте

---

## Управление приложением

### Просмотр логов

```bash
cd ~/var/www/my-site

# Все сервисы
docker-compose logs -f

# Только web приложение
docker-compose logs -f web

# Только nginx
docker-compose logs -f nginx

# Только certbot
docker-compose logs -f certbot
```

**Выход из просмотра логов:** `Ctrl+C`

### Перезапуск приложения

```bash
cd ~/var/www/my-site

# Перезапуск всех контейнеров
docker-compose restart

# Перезапуск конкретного контейнера
docker-compose restart web
docker-compose restart nginx
```

### Остановка приложения

```bash
cd ~/var/www/my-site

# Остановка всех контейнеров
docker-compose down

# Остановка с удалением volumes (ОСТОРОЖНО - удалит данные!)
docker-compose down -v
```

### Запуск после остановки

```bash
cd ~/var/www/my-site
docker-compose up -d
```

### Обновление приложения

**Если используете Git:**

```bash
cd ~/var/www/my-site
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d
docker-compose exec web npm run db:migrate
```

**Если копируете через SCP:**

```bash
# На локальном компьютере
scp -r * admin@82.202.138.157:~/var/www/my-site/

# На сервере
cd ~/var/www/my-site
docker-compose down
docker-compose build --no-cache
docker-compose up -d
docker-compose exec web npm run db:migrate
```

### Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Проверка портов
sudo netstat -tlnp | grep -E ':(80|443|3000)'
```

---

## Резервное копирование

### Создание бэкапа вручную

```bash
cd ~/var/www/my-site

# Запустите скрипт бэкапа
./backup.sh
```

**Скрипт автоматически:**
- ✅ Создаст резервную копию базы данных
- ✅ Создаст резервную копию всех файлов storage
- ✅ Сохранит .env файл
- ✅ Создаст архив с датой и временем
- ✅ Удалит старые бэкапы (старше 7 дней)

**Расположение бэкапов:** `~/backups/portfolio_backup_YYYYMMDD_HHMMSS.tar.gz`

### Автоматические бэкапы (cron)

Настройте автоматические ежедневные бэкапы:

```bash
# Откройте crontab
crontab -e

# Добавьте строку для ежедневного бэкапа в 3:00
0 3 * * * /home/admin/var/www/my-site/backup.sh >> /home/admin/backups/backup.log 2>&1
```

**Примечание:** Замените `/home/admin` на ваш домашний каталог, если используете другого пользователя.

### Восстановление из бэкапа

```bash
# Распакуйте архив бэкапа
cd ~/backups
tar -xzf portfolio_backup_YYYYMMDD_HHMMSS.tar.gz

# Скопируйте файлы обратно
cd ~/var/www/my-site
cp -r ~/backups/portfolio_backup_YYYYMMDD_HHMMSS/* .

# Перезапустите контейнеры
docker-compose restart
```

---

## Решение проблем

### Сайт не открывается

**1. Проверьте статус контейнеров:**
```bash
docker-compose ps
```
Все контейнеры должны быть в статусе "Up". Если нет — проверьте логи.

**2. Проверьте логи:**
```bash
docker-compose logs web
docker-compose logs nginx
```

**3. Проверьте порты:**
```bash
sudo netstat -tlnp | grep -E ':(80|443)'
```
Порты 80 и 443 должны быть открыты.

**4. Проверьте файрвол:**
```bash
sudo ufw status
```
Порты 80 и 443 должны быть разрешены.

**5. Проверьте DNS:**
```bash
nslookup zelyonkin.ru
```
Домен должен указывать на IP сервера (82.202.138.157).

### Ошибка SSL сертификата

**1. Проверьте наличие сертификатов:**
```bash
ls -la certbot/live/zelyonkin.ru/
```
Должны быть файлы `fullchain.pem` и `privkey.pem`.

**2. Перезапустите nginx:**
```bash
docker-compose restart nginx
```

**3. Проверьте конфигурацию nginx:**
```bash
docker-compose exec nginx nginx -t
```

**4. Если сертификат не получен:**
- Убедитесь, что домен указывает на IP сервера
- Проверьте, что порт 80 открыт
- Попробуйте получить сертификат снова: `./setup-ssl.sh`

### База данных не создается

**1. Проверьте права на директорию:**
```bash
sudo chmod -R 777 database/
```

**2. Выполните миграции вручную:**
```bash
docker-compose exec web npm run db:migrate
```

**3. Проверьте .env файл:**
```bash
cat .env | grep DATABASE_URL
```
Должно быть: `DATABASE_URL="file:./database/db.sqlite"`

### Ошибка "Cannot connect to database"

**1. Проверьте `DATABASE_URL` в `.env`**
**2. Убедитесь, что директория `database/` существует**
**3. Проверьте права доступа к файлу базы данных:**
```bash
ls -la database/
```

### Контейнер не запускается

**1. Проверьте логи:**
```bash
docker-compose logs [имя_контейнера]
```

**2. Проверьте использование диска:**
```bash
df -h
```

**3. Пересоберите образ:**
```bash
docker-compose build --no-cache [имя_сервиса]
docker-compose up -d [имя_сервиса]
```

### Проблемы с миграциями

**1. Выполните миграции вручную:**
```bash
docker-compose exec web npm run db:migrate
```

**2. Если миграции не применяются:**
```bash
# Войдите в контейнер
docker-compose exec web sh

# Внутри контейнера
npm run db:migrate
npm run db:generate
```

### Проблемы с правами доступа

**1. Установите права на директории:**
```bash
sudo chmod -R 755 ~/var/www/my-site
sudo chown -R $USER:$USER ~/var/www/my-site
```

**2. Установите права на базу данных:**
```bash
sudo chmod -R 777 database/
```

---

## Безопасность

### Рекомендации по безопасности

1. **✅ Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **✅ Используйте сильные пароли** для админки (не используйте пароль по умолчанию в продакшене!)

3. **✅ Регулярно делайте бэкапы**

4. **✅ Не коммитьте .env файл** в git

5. **✅ Ограничьте доступ к серверу** через SSH ключи:
   ```bash
   # На локальном компьютере
   ssh-keygen -t rsa -b 4096
   ssh-copy-id admin@82.202.138.157
   ```

6. **✅ Включите 2FA** в настройках админки для дополнительной защиты

7. **✅ Регулярно проверяйте логи** на подозрительную активность

8. **✅ Настройте автоматические обновления безопасности:**
   ```bash
   sudo apt install unattended-upgrades
   sudo dpkg-reconfigure -plow unattended-upgrades
   ```

---

## Полезные команды

### Быстрые команды

```bash
# Перейти в директорию проекта
cd ~/var/www/my-site

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Запуск
docker-compose up -d

# Создание бэкапа
./backup.sh

# Получение SSL сертификата
./setup-ssl.sh
```

### Проверка работы

```bash
# Проверка HTTP
curl -I http://zelyonkin.ru

# Проверка HTTPS
curl -I https://zelyonkin.ru

# Проверка SSL сертификата
openssl s_client -connect zelyonkin.ru:443 -servername zelyonkin.ru
```

---

## Контакты и поддержка

При возникновении проблем проверьте:
- ✅ Логи контейнеров (`docker-compose logs -f`)
- ✅ Конфигурацию nginx (`nginx.conf`)
- ✅ Переменные окружения в `.env`
- ✅ Права доступа к файлам
- ✅ Статус контейнеров (`docker-compose ps`)

---

**Версия релиза:** 2.2.0  
**Дата:** 2025-01-XX  
**Домен:** zelyonkin.ru  
**IP сервера:** 82.202.138.157

---

## Чеклист развертывания

- [ ] Проект скопирован на сервер
- [ ] `server-setup.sh` выполнен (один раз)
- [ ] Выход и повторный вход после установки Docker
- [ ] `setup.sh` выполнен успешно
- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] SSL сертификат получен (`./setup-ssl.sh`)
- [ ] Сайт доступен по https://zelyonkin.ru
- [ ] Вход в админку работает
- [ ] Пароль изменен в настройках админки
- [ ] 2FA настроена (опционально)
- [ ] Автоматические бэкапы настроены (cron)

**✅ Развертывание завершено!**
