# My-WebSite2.0

Публичный сайт и приватный кабинет для управления заметками, файлами и идеями.

## 🚀 Возможности

- **Публичная часть**: 
  - Портфолио проектов с детальными страницами
  - Информация о себе
  - Контакты
  - Интеграции GitHub и LeetCode для отображения активности

- **Приватный кабинет**: 
  - Управление заметками с версионированием
  - Управление файлами
  - Vault для важных данных
  - Календарь идей
  - Планировщик задач
  - Напоминания
  - Аналитика и статистика
  - Система избранного

- **Расширенные функции**: 
  - Система тем (темная/светлая)
  - Версионирование заметок
  - Экспорт заметок в Markdown/PDF
  - Совместный доступ к заметкам
  - Аналитика и статистика использования
  - Планировщик задач с приоритетами
  - Система напоминаний
  - Автоматические бэкапы
  - Публичное REST API
  - Webhooks для интеграций

## 📋 Требования

- Node.js 18+
- Docker и Docker Compose
- PostgreSQL (для production)
- Nginx (для production)

## 🛠️ Установка

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd My-WebSite2.0
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp env.example .env
# Отредактируйте .env файл
```

4. Инициализируйте базу данных:
```bash
npx prisma migrate dev
npm run db:seed
```

5. Запустите приложение:
```bash
npm run dev
```

Приложение будет доступно по адресу http://localhost:3000

### Production развертывание

См. [DEPLOYMENT.md](./DEPLOYMENT.md) для детальных инструкций.

## 📁 Структура проекта

```
My-WebSite2.0/
├── prisma/              # Prisma schema и миграции
├── public/              # Статические файлы
├── src/
│   ├── app/            # Next.js App Router
│   ├── components/     # React компоненты
│   ├── lib/            # Утилиты и библиотеки
│   ├── hooks/          # React hooks
│   └── styles/         # Стили
├── scripts/            # Скрипты для развертывания
├── storage/            # Загруженные файлы
└── docker-compose.yml  # Docker Compose конфигурация
```

## 🔧 Скрипты

### NPM скрипты
- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для production
- `npm run start` - Запуск production сервера (standalone mode, требует предварительной сборки с NODE_ENV=production)
- `npm run start:dev` - Запуск dev сервера (альтернатива для разработки)
- `npm run migrate` - Применение миграций
- `npm run db:seed` - Инициализация админа
- `npm run db:init` - Инициализация production БД
- `npm run db:studio` - Открыть Prisma Studio

### Скрипты развертывания
- `./scripts/setup-server.sh` - Первоначальная настройка сервера
- `./scripts/install-cert.sh` - Установка SSL сертификата
- `./scripts/deploy.sh` - Развертывание приложения
- `./scripts/setup-complete.sh` - Полная автоматическая настройка
- `./scripts/update.sh` - Обновление приложения
- `./scripts/backup-db.sh` - Бэкап базы данных
- `./scripts/monitor.sh` - Мониторинг приложения
- `./scripts/logs.sh [service]` - Просмотр логов

## 🔐 Переменные окружения

Основные переменные (см. `env.example`):

- `DATABASE_URL` - URL базы данных
- `NEXTAUTH_URL` - URL приложения
- `NEXTAUTH_SECRET` - Секретный ключ для NextAuth
- `ADMIN_EMAIL` - Email администратора
- `ADMIN_USERNAME` - Имя пользователя администратора
- `ADMIN_PASSWORD` - Пароль администратора

## 📚 Документация

- [Инструкции по развертыванию](./DEPLOYMENT.md) - Детальное руководство по развертыванию на Ubuntu 22.04
- [Changelog](./CHANGELOG.md) - История изменений
- [Contributing](./CONTRIBUTING.md) - Руководство по внесению вклада

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd My-WebSite2.0

# Быстрый старт

## Windows (PowerShell)
```powershell
# Автоматическая настройка
.\scripts\setup-dev-env.ps1

# Или вручную
npm install
Copy-Item env.example .env
# Отредактируйте .env и установите DATABASE_URL
# Для Docker Compose:
docker-compose up -d postgres
# Или используйте локальный PostgreSQL
npx prisma migrate dev
npm run db:seed
npm run dev
```

## Linux/Mac
```bash
chmod +x scripts/quick-start.sh
./scripts/quick-start.sh

# Или вручную
npm install
cp env.example .env
# Отредактируйте .env и установите DATABASE_URL
npx prisma migrate dev
npm run db:seed
npm run dev
```
```

### Production развертывание

```bash
# На сервере Ubuntu 22.04
./scripts/setup-complete.sh
```

См. [DEPLOYMENT.md](./DEPLOYMENT.md) для детальных инструкций.

## 🤝 Вклад

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Приватный проект.

## 👤 Автор

skre4karta (zelyonkin.d@gmail.com)
