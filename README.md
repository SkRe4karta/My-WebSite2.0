# zelyonkin.ru — версия 2.2.1

Портфолио сайт на Next.js 16 с публичной визиткой и приватной админ-панелью. Готов к деплою на Ubuntu 22.04 + Docker + Nginx + SSL.

## 📦 Стек

- **Frontend:** Next.js 16 (App Router) + React 19 + Tailwind CSS v4
- **Backend:** Next.js API Routes
- **Auth:** NextAuth.js (Credentials) + 2FA/TOTP
- **DB:** Prisma + SQLite
- **Storage:** Локальные файлы + AES-256-GCM Vault
- **Deploy:** Docker, docker-compose, Nginx, Certbot

## 📁 Структура проекта

```
my-portfolio-site/
├── .env.example              # Пример переменных окружения
├── .dockerignore            # Исключения для Docker build
├── Dockerfile                # Multi-stage Docker образ
├── docker-compose.yml        # Оркестрация контейнеров
├── nginx.conf                # Конфигурация Nginx
├── next.config.ts            # Настройки Next.js
├── package.json
├── install.sh                # ⭐ Главный скрипт установки
├── deploy.sh                 # Скрипт развертывания
├── setup-env.sh              # Генерация .env
├── setup-ssl.sh              # Получение SSL сертификата
├── server-setup.sh           # Настройка сервера Ubuntu
├── backup.sh                 # Резервное копирование
├── setup.sh                  # Перенаправление на install.sh
├── database/                 # SQLite база данных
├── storage/                  # Хранилище файлов
│   ├── uploads/             # Загруженные файлы
│   └── vault/               # Шифрованные файлы
├── prisma/                   # Prisma ORM + миграции
├── public/                   # Статические файлы
└── src/                      # Исходный код
    ├── app/                  # Next.js App Router
    ├── components/           # React компоненты
    ├── lib/                  # Утилиты
    └── styles/               # Стили
```

## 🔧 Скрипты проекта

### install.sh — Главный скрипт установки

**Назначение:** Единый скрипт для полной установки и развертывания проекта.

**Что делает:**
1. Проверяет окружение (Docker, Docker Compose)
2. Создает `.env` файл через `setup-env.sh` (если не существует)
3. Создает необходимые директории (database, storage, certbot)
4. Устанавливает права на все скрипты
5. Останавливает старые контейнеры
6. Собирает Docker образы (`docker compose build --no-cache`)
7. Запускает контейнеры (`docker compose up -d`)
8. Ожидает запуск приложения (проверка healthcheck до 60 секунд)
9. Выполняет миграции базы данных

**Использование:**
```bash
chmod +x install.sh
./install.sh
```

**Время выполнения:** 5-15 минут (зависит от скорости интернета)

### deploy.sh — Скрипт развертывания

**Назначение:** Развертывание или обновление приложения.

**Опции:**
- Без аргументов: полная пересборка и развертывание
- `--no-build`: обновление без пересборки образов (быстрее)

**Что делает:**
1. Проверяет наличие `.env` файла
2. Проверяет обязательные переменные окружения
3. Проверяет Docker и Docker Compose
4. Создает необходимые директории
5. Останавливает существующие контейнеры
6. Собирает образы (если не `--no-build`)
7. Запускает контейнеры
8. Ожидает healthcheck
9. Выполняет миграции
10. Проверяет статус контейнеров

**Использование:**
```bash
./deploy.sh              # Полная пересборка
./deploy.sh --no-build   # Обновление без пересборки
```

### setup-env.sh — Генерация .env файла

**Назначение:** Автоматическая генерация файла `.env` с переменными окружения.

**Что делает:**
1. Запрашивает пароль администратора (по умолчанию: `1234`)
2. Генерирует bcrypt хэш пароля
3. Генерирует `NEXTAUTH_SECRET` (openssl rand -base64 32)
4. Генерирует `VAULT_ENCRYPTION_KEY` (openssl rand -base64 32)
5. Генерирует `ENCRYPTED_STORAGE_SALT` (openssl rand -base64 16)
6. Создает `.env` файл со всеми переменными

**Использование:**
```bash
chmod +x setup-env.sh
./setup-env.sh
```

**Важно:** После первого входа обязательно смените пароль в настройках админки!

### setup-ssl.sh — Получение SSL сертификата

**Назначение:** Получение SSL сертификата от Let's Encrypt для домена.

**Требования:**
- Контейнеры должны быть запущены (`./deploy.sh`)
- Домен должен указывать на IP сервера
- Порты 80 и 443 должны быть открыты

**Что делает:**
1. Проверяет, что Nginx контейнер запущен
2. Останавливает Nginx временно
3. Запускает certbot в standalone режиме
4. Получает сертификат для домена и www поддомена
5. Запускает Nginx обратно
6. Перезапускает Nginx для применения сертификата

**Использование:**
```bash
./setup-ssl.sh
```

**Результат:** Сертификат сохраняется в `certbot/live/zelyonkin.ru/` и автоматически обновляется каждые 12 часов через контейнер certbot.

### server-setup.sh — Настройка сервера

**Назначение:** Первоначальная настройка Ubuntu 22.04 сервера.

**Что делает:**
1. Обновляет систему (`apt update && apt upgrade`)
2. Устанавливает базовые пакеты (curl, git, openssl, ufw)
3. Устанавливает Docker (через официальный скрипт)
4. Добавляет пользователя в группу docker
5. Устанавливает Docker Compose
6. Настраивает файрвол (открывает порты 22, 80, 443)
7. Создает директорию `~/var/www` для проекта

**Использование:**
```bash
chmod +x server-setup.sh
./server-setup.sh
```

**Важно:** После установки Docker необходимо выйти и войти снова через SSH для применения изменений группы.

### backup.sh — Резервное копирование

**Назначение:** Создание резервной копии проекта.

**Что копируется:**
- База данных (`database/`)
- Файлы storage (`storage/`)
- Файл `.env`

**Где хранится:** `~/backups/portfolio_backup_YYYYMMDD_HHMMSS.tar.gz`

**Что делает:**
1. Создает директорию для бэкапов
2. Копирует database, storage, .env
3. Создает tar.gz архив
4. Удаляет старые бэкапы (старше 7 дней)

**Использование:**
```bash
./backup.sh
```

### setup.sh — Перенаправление

**Назначение:** Перенаправляет на `install.sh` для единообразия.

**Использование:**
```bash
./setup.sh  # Эквивалентно ./install.sh
```

### generate-password-hash.sh — Генерация хеша пароля

**Назначение:** Генерация bcrypt хеша пароля для добавления в `.env`.

**Использование:**
```bash
./generate-password-hash.sh        # Генерирует хеш для пароля "1234"
./generate-password-hash.sh mypass # Генерирует хеш для указанного пароля
```

**Что делает:**
- Использует Docker контейнер или локальный Node.js
- Генерирует bcrypt хеш (10 rounds)
- Выводит хеш для копирования в `.env` файл

## ⚙️ Конфигурационные файлы

### Dockerfile

**Тип:** Multi-stage build (3 стадии)

**Стадии:**
1. **base:** Базовый образ с Node.js 20-slim + openssl, ca-certificates
2. **deps:** Установка зависимостей + генерация Prisma Client
3. **builder:** Копирование node_modules + исправление прав (`chmod +x node_modules/.bin`) + сборка Next.js
4. **runner:** Финальный образ с минимальным размером + non-root пользователь

**Ключевые особенности:**
- Исправление прав доступа для node_modules/.bin (критично для работы)
- Healthcheck endpoint (`/api/health`)
- Standalone output для уменьшения размера
- Non-root пользователь для безопасности

### docker-compose.yml

**Сервисы:**
- **web:** Next.js приложение (порт 3000 внутри, healthcheck)
- **nginx:** Веб-сервер (порты 80, 443, зависит от web)
- **certbot:** Автоматическое обновление SSL (каждые 12 часов)

**Volumes:**
- `./database:/app/database` — база данных
- `./storage:/app/storage` — файлы
- `./certbot:/etc/letsencrypt` — SSL сертификаты

**Networks:** `portfolio_network` (bridge)

### nginx.conf

**Оптимизации:**
- Gzip компрессия (уровень 6)
- Кеширование статических файлов (_next/static, изображения)
- Rate limiting (общий: 10r/s, API: 5r/s, логин: 2r/s)
- Современная SSL конфигурация (TLS 1.2/1.3)
- HTTP/2 поддержка
- Security headers (HSTS, X-Frame-Options, CSP)

**Серверы:**
- HTTP (порт 80): редирект на HTTPS, кроме /.well-known/acme-challenge/
- HTTPS (порт 443): основной сервер с SSL

### next.config.ts

**Оптимизации:**
- Standalone output для Docker
- Сжатие (compress: true)
- Оптимизация изображений (AVIF, WebP)
- Code splitting (Webpack оптимизации)
- Security headers (CSP, HSTS, X-Frame-Options)
- Кеширование статических ресурсов

## 🚀 Развертывание на ВМ

### Шаг 1: Подготовка проекта

Убедитесь, что в проекте есть все необходимые файлы:
- Все скрипты (`.sh` файлы)
- Конфигурационные файлы (Dockerfile, docker-compose.yml, nginx.conf)
- Исходный код (src/, public/, prisma/)
- package.json и package-lock.json

**Не нужны для переноса:**
- node_modules/ (установятся при сборке)
- .next/ (создастся при сборке)
- database/*.sqlite (создастся при первом запуске)
- storage/uploads/* и storage/vault/* (создадутся автоматически)
- .env файлы (создадутся на сервере)

### Шаг 2: Копирование на сервер

**С локального компьютера (Windows PowerShell):**
```powershell
cd C:\Users\zelyo\.vscode\my-portfolio-site
scp -r * admin@82.202.138.157:~/var/www/my-site/
```

**Или через Git (если проект в репозитории):**
```bash
# На сервере
cd ~/var/www
git clone <repository-url> my-site
cd my-site
```

### Шаг 3: Настройка сервера (первый раз)

**На сервере:**
```bash
cd ~/var/www/my-site
chmod +x *.sh
./server-setup.sh
```

**Если Docker был установлен, выйдите и войдите снова:**
```bash
exit
ssh admin@82.202.138.157
cd ~/var/www/my-site
```

**Проверьте установку:**
```bash
docker --version
docker-compose --version
```

### Шаг 4: Установка проекта

```bash
cd ~/var/www/my-site
./install.sh
```

Скрипт автоматически:
- Создаст `.env` файл (пароль по умолчанию: `1234`)
- Создаст все директории
- Соберет Docker образы
- Запустит контейнеры
- Выполнит миграции

**Время выполнения:** 5-15 минут

### Шаг 5: Настройка SSL

```bash
./setup-ssl.sh
```

**Требования:**
- Домен `zelyonkin.ru` должен указывать на IP сервера
- Порты 80 и 443 открыты в файрволе

### Шаг 6: Проверка работы

```bash
# Проверка статуса контейнеров
docker-compose ps

# Проверка healthcheck
curl http://localhost/api/health

# Просмотр логов
docker-compose logs -f web
```

**Сайт будет доступен:**
- HTTP: `http://zelyonkin.ru` (редирект на HTTPS)
- HTTPS: `https://zelyonkin.ru`

### Шаг 7: Первый вход

1. Откройте `https://zelyonkin.ru/login`
2. Логин: `skre4karta`
3. Пароль: `1234` (по умолчанию)
4. **ВАЖНО:** Сразу смените пароль в настройках админки!

## ⚙️ Переменные окружения

Создайте `.env` на основе `.env.example` или используйте `./setup-env.sh`.

**Обязательные переменные:**
- `NEXTAUTH_URL` — URL сайта (https://zelyonkin.ru)
- `NEXTAUTH_SECRET` — секретный ключ (openssl rand -base64 32)
- `ADMIN_USERNAME` — имя администратора
- `ADMIN_EMAIL` — email администратора
- `ADMIN_PASSWORD_HASH` — bcrypt хэш пароля
- `DATABASE_URL` — путь к БД (file:./database/db.sqlite)
- `VAULT_ENCRYPTION_KEY` — ключ шифрования (openssl rand -base64 32)
- `ENCRYPTED_STORAGE_SALT` — соль (openssl rand -base64 16)
- `NEXT_PUBLIC_ADMIN_USERNAME` — публичное имя
- `NODE_ENV` — production

## 📋 Управление приложением

**Просмотр логов:**
```bash
docker-compose logs -f          # Все контейнеры
docker-compose logs -f web      # Только web
docker-compose logs -f nginx    # Только nginx
```

**Остановка:**
```bash
docker-compose down
```

**Перезапуск:**
```bash
docker-compose restart
docker-compose restart web      # Только web контейнер
```

**Обновление без пересборки:**
```bash
./deploy.sh --no-build
```

**Резервное копирование:**
```bash
./backup.sh
```

**Проверка healthcheck:**
```bash
curl http://localhost/api/health
curl https://zelyonkin.ru/api/health
```

## 🔧 Решение проблем

**Проблема: "Permission denied" при сборке Docker**
- Решение: Dockerfile исправлен (добавлен `chmod +x node_modules/.bin`)

**Проблема: "Docker daemon не запущен"**
- Решение: `sudo systemctl start docker` или проверьте права: `sudo usermod -aG docker $USER`

**Проблема: "Healthcheck не проходит"**
- Решение: Подождите 40-60 секунд после запуска, проверьте логи: `docker-compose logs web`

**Проблема: "SSL сертификат не получен"**
- Решение: Проверьте, что домен указывает на IP сервера и порты 80/443 открыты

**Проблема: "Миграции не применяются"**
- Решение: Выполните вручную: `docker-compose exec web npm run db:migrate`

## 📝 Полезные команды

```bash
# Полная пересборка
./deploy.sh

# Обновление без пересборки
./deploy.sh --no-build

# Получение SSL
./setup-ssl.sh

# Резервное копирование
./backup.sh

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Вход в контейнер
docker-compose exec web sh

# Выполнение команд в контейнере
docker-compose exec web npm run db:migrate
docker-compose exec web npm run db:studio
```

## 🔐 Безопасность

- Хеширование паролей (bcrypt)
- 2FA/TOTP аутентификация
- Защита от брутфорса
- Content Security Policy
- Система аудита безопасности
- Rate limiting в Nginx
- Security headers

## 🔧 NPM скрипты для работы с БД

### Управление базой данных

```bash
# Миграции
npm run db:migrate          # Применить миграции (prisma migrate deploy)
npm run db:migrate:dev      # Разработка (prisma migrate dev)
npm run db:generate         # Генерация Prisma Client
npm run db:studio           # Открыть Prisma Studio (http://localhost:5555)
```

### Управление пользователями

```bash
# Создание/обновление администратора
npm run db:init-admin       # Создать или обновить администратора из .env

# Диагностика и исправление
npm run db:check-auth       # Проверить настройки аутентификации
npm run db:test-login       # Протестировать вход (логин: skre4karta, пароль: 1234)
npm run db:fix-user         # Исправить пользователя (обновить name и пароль)
npm run db:force-fix-user   # Принудительное исправление (генерирует новый хеш)
```

## 📚 Документация

### Основные документы

- **`START-HERE.md`** ⭐ - Начните отсюда! Быстрый старт для развертывания
- **`DEPLOYMENT-GUIDE.md`** - Полное руководство по развертыванию
- **`README.md`** - Этот файл, общая документация проекта
- **`RELEASE.md`** - Инструкция по развертыванию релизной версии

### Быстрые инструкции

- **`QUICK-DEPLOY-COMMANDS.md`** - Быстрые команды для развертывания
- **`COMMANDS.md`** - Все команды проекта
- **`GIT-COMMIT.md`** - Инструкция по коммиту и отправке в git

### Обновления и изменения

- **`DEPLOY-UPDATE.md`** - Инструкция по обновлению на ВМ
- **`UPDATE-SUMMARY.md`** - Краткая сводка изменений версии 2.2.1
- **`CHANGELOG.md`** - История всех изменений проекта

### Диагностика и решение проблем

- **`DIAGNOSE-LOGIN.md`** - Диагностика проблем с входом
- **`FIX_NGINX.md`** - Исправление проблем с Nginx

### Чеклисты

- **`FINAL-CHECKLIST.md`** - Финальный чеклист перед развертыванием
- **`DEPLOY-CHECKLIST.md`** - Чеклист развертывания
- **`PREPARE-FOR-DEPLOY.md`** - Подготовка к развертыванию

### Дополнительные документы

- **`README-DEPLOY.md`** - Краткая инструкция по развертыванию
- **`README-UPDATE.md`** - Инструкция по обновлению
- **`QUICKSTART.md`** - Быстрый старт
- **`DEPLOYMENT.md`** - Развертывание
- **`DEPLOY.md`** - Развертывание (альтернативная версия)
- **`STATUS.md`** - Статус проекта
- **`GIT-SETUP.md`** - Настройка git

Подробное описание всех скриптов и документов см. в **`PROJECT-STRUCTURE.md`**.

## 📄 Версия

**Версия:** 2.2.1  
**Домен:** zelyonkin.ru  
**Дата:** Январь 2025

### Что нового в версии 2.2.1

- ✅ Управление пользователями (CRUD в настройках)
- ✅ Исправлена аутентификация (поддержка множественных пользователей)
- ✅ Диагностические скрипты для отладки
- ✅ Автоматическое создание администратора
- ✅ Исправлены миграции базы данных
