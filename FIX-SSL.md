# 🔒 Исправление проблем с SSL

## Проблема: Не удается получить SSL сертификат

### Симптомы:
- Ошибка "Connection refused" при получении сертификата
- Certbot не может пройти проверку домена
- Порт 80 недоступен для Let's Encrypt

## ✅ Решение

### Шаг 1: Проверка предварительных условий

```bash
cd /var/www/zelyonkin.ru

# 1. Проверка, что домен указывает на сервер
nslookup zelyonkin.ru
# Должен показать IP: 82.202.138.157

# 2. Проверка, что порты открыты
sudo ufw status
# Порты 80 и 443 должны быть открыты

# 3. Проверка, что nginx запущен
docker-compose ps nginx
# Должен быть в статусе "Up"

# 4. Проверка доступности порта 80
curl -I http://zelyonkin.ru
# Должен вернуть HTTP ответ
```

### Шаг 2: Использование webroot метода (рекомендуется)

Скрипт `setup-ssl.sh` теперь использует webroot метод, который **не требует остановки nginx**:

```bash
cd /var/www/zelyonkin.ru

# Запустите скрипт
./setup-ssl.sh
```

**Преимущества webroot метода:**
- ✅ Не требует остановки nginx
- ✅ Работает пока сайт работает
- ✅ Более надежный

### Шаг 3: Проверка конфигурации nginx

Убедитесь, что в `nginx.conf` есть блок для acme-challenge:

```nginx
location /.well-known/acme-challenge/ {
  root /var/www/certbot;
  allow all;
}
```

### Шаг 4: Проверка директории certbot/www

```bash
# Проверка существования директории
ls -la certbot/www/

# Если не существует, создайте:
mkdir -p certbot/www
chmod 755 certbot/www
```

### Шаг 5: Ручное получение сертификата (если скрипт не работает)

```bash
cd /var/www/zelyonkin.ru

# Убедитесь, что nginx запущен
docker-compose up -d nginx

# Получите сертификат вручную
docker-compose run --rm --entrypoint "" certbot sh -c "certbot certonly \
    --webroot \
    --webroot-path /var/www/certbot \
    --preferred-challenges http \
    -d zelyonkin.ru \
    -d www.zelyonkin.ru \
    --email zelyonkin.d@gmail.com \
    --agree-tos \
    --non-interactive"
```

### Шаг 6: Проверка сертификата

```bash
# Проверка наличия сертификата
ls -la certbot/live/zelyonkin.ru/

# Должны быть файлы:
# - fullchain.pem
# - privkey.pem
```

### Шаг 7: Настройка HTTPS в nginx.conf

После получения сертификата нужно раскомментировать HTTPS блок в `nginx.conf`:

```nginx
server {
  listen 443 ssl http2;
  server_name zelyonkin.ru www.zelyonkin.ru;
  
  ssl_certificate /etc/letsencrypt/live/zelyonkin.ru/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/zelyonkin.ru/privkey.pem;
  
  # ... остальная конфигурация
}
```

И добавить редирект с HTTP на HTTPS:

```nginx
server {
  listen 80;
  server_name zelyonkin.ru www.zelyonkin.ru;
  
  # Редирект на HTTPS (кроме acme-challenge)
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
    allow all;
  }
  
  location / {
    return 301 https://$host$request_uri;
  }
}
```

### Шаг 8: Перезапуск nginx

```bash
# Проверка конфигурации
docker-compose exec nginx nginx -t

# Перезапуск
docker-compose restart nginx
```

## 🔍 Диагностика проблем

### Проблема: "Connection refused"

**Причина:** Порт 80 недоступен извне

**Решение:**
```bash
# Проверка файрвола
sudo ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверка, что nginx слушает порт 80
sudo netstat -tlnp | grep :80
```

### Проблема: "Domain does not point to this server"

**Причина:** DNS не настроен правильно

**Решение:**
```bash
# Проверка DNS
nslookup zelyonkin.ru
dig zelyonkin.ru

# Должен показывать IP: 82.202.138.157
```

### Проблема: "Rate limit exceeded"

**Причина:** Слишком много попыток получить сертификат

**Решение:**
- Подождите 1 час
- Или используйте `--dry-run` для тестирования

### Проблема: "Challenge failed"

**Причина:** Nginx не может отдать файлы из `/var/www/certbot`

**Решение:**
```bash
# Проверка прав на директорию
ls -la certbot/www/
chmod 755 certbot/www

# Проверка, что nginx может читать файлы
docker-compose exec nginx ls -la /var/www/certbot/
```

## 📝 Автоматическое обновление

Сертификат будет автоматически обновляться каждые 12 часов через контейнер `portfolio_certbot`. Никаких дополнительных действий не требуется.

## ✅ Проверка после настройки

```bash
# Проверка HTTPS
curl -I https://zelyonkin.ru

# Проверка сертификата
openssl s_client -connect zelyonkin.ru:443 -servername zelyonkin.ru < /dev/null
```

---

**Версия:** 2.2.1  
**Дата:** 2025-01-18

