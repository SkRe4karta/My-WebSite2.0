# 🚀 Инструкция по обновлению проекта на ВМ

## Версия 2.2.1 - Управление пользователями и исправление аутентификации

### 📦 Что нужно сделать

## Шаг 1: Подготовка на локальной машине

### 1.1. Проверка изменений
```bash
cd my-portfolio-site
git status
```

### 1.2. Добавление файлов в git
```bash
# Добавить все изменения
git add .

# Или выборочно:
git add scripts/
git add src/app/api/users/
git add src/components/admin/UserManagement.tsx
git add src/lib/auth.ts src/lib/db.ts
git add package.json Dockerfile install.sh deploy.sh
git add *.md
```

### 1.3. Создание коммита
```bash
git commit -m "feat: управление пользователями и исправление аутентификации (v2.2.1)

Основные изменения:
- Исправлена аутентификация NextAuth (поддержка множественных пользователей)
- Добавлено управление пользователями в настройках админки
- Исправлены миграции базы данных (prisma migrate deploy)
- Добавлены диагностические скрипты для отладки
- Автоматическое создание администратора при установке
- Улучшена конфигурация Nginx для Next.js
- Исправлена инициализация пользователя (name = skre4karta)

Новые возможности:
- CRUD операции для пользователей через API
- UI компонент UserManagement в настройках
- 5 диагностических npm скриптов
- Автоматическое исправление пользователей

Исправления:
- Убрана привязка к ADMIN_USERNAME в аутентификации
- Убран PrismaAdapter при JWT strategy
- Исправлена работа сессий
- Улучшена обработка ошибок миграций"
```

### 1.4. Отправка в репозиторий
```bash
git push origin main
```

## Шаг 2: Обновление на ВМ

### 2.1. Подключение к серверу
```bash
ssh admin@82.202.138.157
```

### 2.2. Обновление кода
```bash
cd /var/www/zelyonkin.ru

# Сохраните текущее состояние (опционально)
cp .env .env.backup

# Получите обновления
git pull origin main
```

### 2.3. Остановка контейнеров
```bash
docker-compose down
```

### 2.4. Пересборка и запуск
```bash
# Пересоберите образы
docker-compose build --no-cache

# Запустите контейнеры
docker-compose up -d

# Дождитесь запуска
sleep 15
```

### 2.5. Применение миграций
```bash
# Примените миграции
docker-compose exec web npm run db:migrate
```

### 2.6. Исправление пользователя
```bash
# Исправьте пользователя (обновит name и пароль)
docker-compose exec web npm run db:force-fix-user
```

### 2.7. Проверка
```bash
# Проверьте статус контейнеров
docker-compose ps

# Проверьте логи
docker-compose logs web | tail -50

# Проверьте настройки аутентификации
docker-compose exec web npm run db:check-auth

# Протестируйте вход
docker-compose exec web npm run db:test-login
```

## Шаг 3: Проверка работы

### 3.1. Проверка сайта
```bash
# Проверьте healthcheck
curl http://localhost/api/health

# Проверьте доступность
curl -I http://localhost
```

### 3.2. Проверка входа
1. Откройте браузер: `http://82.202.138.157` или `http://zelyonkin.ru`
2. Перейдите на `/login`
3. Войдите с:
   - **Логин:** `skre4karta` или `zelyonkin.d@gmail.com`
   - **Пароль:** `1234`

### 3.3. Проверка управления пользователями
1. Войдите в админку
2. Перейдите в **Настройки**
3. В правой колонке найдите **"Управление пользователями"**
4. Попробуйте создать нового пользователя

## 🔧 Если что-то пошло не так

### Проблема: Не работает вход

```bash
# 1. Исправьте пользователя
docker-compose exec web npm run db:force-fix-user

# 2. Проверьте результат
docker-compose exec web npm run db:test-login

# 3. Проверьте логи
docker-compose logs -f web
```

### Проблема: Ошибки миграций

```bash
# Проверьте базу данных
docker-compose exec web npx prisma studio
# Откройте http://localhost:5555

# Или примените миграции вручную
docker-compose exec web npx prisma migrate deploy
```

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs web
docker-compose logs nginx

# Перезапустите
docker-compose restart
```

## 📋 Чеклист обновления

- [ ] Код обновлен на локальной машине
- [ ] Изменения закоммичены и отправлены в git
- [ ] На ВМ выполнен `git pull`
- [ ] Контейнеры пересобраны
- [ ] Миграции применены
- [ ] Пользователь исправлен (`db:force-fix-user`)
- [ ] Проверен вход в админку
- [ ] Проверено управление пользователями
- [ ] Сайт доступен и работает

## 📚 Дополнительная документация

- `DEPLOY-UPDATE.md` - подробная инструкция по обновлению
- `DIAGNOSE-LOGIN.md` - диагностика проблем с входом
- `GIT-COMMIT.md` - команды для git
- `CHANGELOG.md` - история всех изменений

## ⚠️ Важные замечания

1. **Резервная копия:** Перед обновлением сделайте бэкап:
   ```bash
   ./backup.sh
   ```

2. **Переменные окружения:** Убедитесь, что `.env` файл не изменился после `git pull`

3. **База данных:** Миграции применяются автоматически, но проверьте результат

4. **Пароли:** После обновления все пароли должны быть в bcrypt формате

## 🎉 После успешного обновления

1. Войдите в админку
2. Перейдите в **Настройки → Управление пользователями**
3. Создайте дополнительных пользователей (если нужно)
4. Смените пароль администратора на более безопасный
5. Проверьте работу всех функций

---

**Версия:** 2.2.1  
**Дата обновления:** 2025-01-18

