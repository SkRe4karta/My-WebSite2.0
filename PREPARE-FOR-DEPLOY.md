# Подготовка проекта к загрузке на ВМ и выкладке в Git

## ✅ Проект готов к развертыванию

Проект полностью подготовлен к:
- Загрузке на ВМ через SCP
- Выкладке в Git репозиторий

## 📋 Что было сделано

### 1. Очистка проекта
- ✅ Удален `prisma/database/db.sqlite` (база создастся при первом запуске)
- ✅ Созданы `.gitkeep` файлы в пустых директориях
- ✅ Обновлен `.gitignore` для исключения всех ненужных файлов
- ✅ Обновлен `.dockerignore` для оптимизации сборки
- ✅ Создан `.gitattributes` для правильной обработки файлов в Git

### 2. Конфигурации
- ✅ Dockerfile исправлен (добавлен chmod для node_modules/.bin)
- ✅ Все скрипты проверены и готовы
- ✅ .env.example создан с полной документацией

### 3. Документация
- ✅ README.md обновлен с описанием всех скриптов
- ✅ GIT-SETUP.md создан (инструкция по настройке Git)
- ✅ DEPLOY-CHECKLIST.md создан (чеклист проверки)

## 🚀 Загрузка на ВМ

### Способ 1: Через SCP

```powershell
# С локального компьютера (Windows PowerShell)
cd C:\Users\zelyo\.vscode\my-portfolio-site
scp -r * admin@82.202.138.157:~/var/www/my-site/
```

### Способ 2: Через Git

```bash
# На сервере
cd ~/var/www
git clone https://github.com/your-username/my-portfolio-site.git my-site
cd my-site
chmod +x *.sh
./install.sh
```

## 📤 Выкладка в Git

### Инициализация репозитория

```bash
# В директории проекта
cd C:\Users\zelyo\.vscode\my-portfolio-site

# Инициализация (если еще не инициализирован)
git init

# Проверка статуса
git status

# Добавление всех файлов
git add .

# Проверка, что будет закоммичено (убедитесь, что нет .env, node_modules, .next)
git status

# Коммит
git commit -m "Initial commit: zelyonkin.ru v2.2.0 - готов к развертыванию"

# Добавление remote (замените на ваш репозиторий)
git remote add origin https://github.com/your-username/my-portfolio-site.git

# Отправка
git branch -M main
git push -u origin main
```

## ⚠️ Что НЕ будет закоммичено

Следующие файлы автоматически исключены через `.gitignore`:

- `node_modules/` - зависимости
- `.next/` - build артефакты
- `.env*` - файлы окружения (кроме `.env.example`)
- `database/*.sqlite` - база данных
- `prisma/database/*.sqlite` - база данных Prisma
- `storage/uploads/*` - загруженные файлы
- `storage/vault/*` - зашифрованные файлы
- `certbot/live/`, `certbot/archive/`, `certbot/renewal/`, `certbot/logs/` - SSL сертификаты

## ✅ Финальная проверка

Перед загрузкой на ВМ или выкладкой в Git убедитесь:

1. ✅ Все скрипты на месте и исполняемые
2. ✅ .env.example существует
3. ✅ Нет .env файлов в проекте
4. ✅ Нет баз данных (*.sqlite)
5. ✅ Нет node_modules и .next
6. ✅ .gitignore настроен правильно
7. ✅ README.md обновлен

## 📝 Следующие шаги

### После загрузки на ВМ:

1. Подключитесь к серверу: `ssh admin@82.202.138.157`
2. Перейдите в директорию: `cd ~/var/www/my-site`
3. Запустите установку: `./install.sh`
4. Получите SSL: `./setup-ssl.sh`

### После выкладки в Git:

1. Другие разработчики могут клонировать: `git clone <repository-url>`
2. Создать .env: `./setup-env.sh`
3. Развернуть: `./install.sh`

---

**Проект готов к использованию!** 🎉

