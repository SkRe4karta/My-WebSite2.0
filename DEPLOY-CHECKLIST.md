# Чеклист подготовки к развертыванию

## ✅ Проверка перед загрузкой на ВМ

### Файлы и директории

- [x] Все скрипты на месте (install.sh, deploy.sh, setup-env.sh и др.)
- [x] Конфигурационные файлы (Dockerfile, docker-compose.yml, nginx.conf)
- [x] Исходный код (src/, public/, prisma/)
- [x] package.json и package-lock.json
- [x] .env.example создан
- [x] .gitignore настроен правильно
- [x] .gitattributes создан

### Удалено (не нужно для переноса)

- [x] node_modules/ (установятся при сборке)
- [x] .next/ (создастся при сборке)
- [x] database/*.sqlite (создастся при первом запуске)
- [x] prisma/database/*.sqlite (создастся при первом запуске)
- [x] storage/uploads/* (создадутся автоматически)
- [x] storage/vault/* (создадутся автоматически)
- [x] .env файлы (создадутся на сервере)
- [x] certbot/live, archive, renewal, logs (получатся на сервере)

### Структура директорий

- [x] database/ существует (пустая, с .gitkeep)
- [x] storage/uploads/ существует (пустая, с .gitkeep)
- [x] storage/vault/ существует (пустая, с .gitkeep)
- [x] certbot/ существует (только README.md)

### Конфигурации

- [x] Dockerfile исправлен (chmod для node_modules/.bin)
- [x] docker-compose.yml настроен
- [x] nginx.conf оптимизирован
- [x] next.config.ts оптимизирован
- [x] .dockerignore настроен правильно

## ✅ Проверка перед выкладкой в Git

### Git настройки

- [x] .gitignore настроен правильно
- [x] .gitattributes создан
- [x] .env.example включен в репозиторий
- [x] .env файлы исключены
- [x] Базы данных исключены
- [x] node_modules исключены
- [x] .next исключен

### Документация

- [x] README.md обновлен
- [x] GIT-SETUP.md создан
- [x] DEPLOY-CHECKLIST.md создан

## 📋 Команды для Git

```bash
# Инициализация (если еще не инициализирован)
git init

# Проверка статуса
git status

# Добавление всех файлов
git add .

# Проверка, что будет закоммичено
git status

# Коммит
git commit -m "Initial commit: zelyonkin.ru v2.2.0 - готов к развертыванию"

# Добавление remote (замените на ваш репозиторий)
git remote add origin https://github.com/your-username/my-portfolio-site.git

# Отправка
git branch -M main
git push -u origin main
```

## 📋 Команды для загрузки на ВМ

```bash
# С локального компьютера (Windows PowerShell)
cd C:\Users\zelyo\.vscode\my-portfolio-site
scp -r * admin@82.202.138.157:~/var/www/my-site/

# Или через Git на сервере
ssh admin@82.202.138.157
cd ~/var/www
git clone https://github.com/your-username/my-portfolio-site.git my-site
cd my-site
./install.sh
```

## ⚠️ Важно

1. **НЕ коммитьте** .env файлы
2. **НЕ коммитьте** базы данных
3. **НЕ коммитьте** node_modules
4. **НЕ коммитьте** SSL сертификаты
5. **НЕ коммитьте** загруженные файлы

Все это будет создано автоматически на сервере при развертывании.

