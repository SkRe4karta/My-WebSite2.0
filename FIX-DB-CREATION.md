# 🔧 Исправление проблемы: База данных не создается

## Проблема

Миграции показывают успех, но файл `database/db.sqlite` не создается.

**Симптомы:**
- Миграции выполнены успешно: `All migrations have been successfully applied`
- Но файл `database/db.sqlite` отсутствует
- В контейнере в `/app/database/` только `.gitkeep`

## ✅ Решение

### Шаг 1: Проверка прав доступа

```bash
cd /var/www/zelyonkin.ru

# Проверьте права на директорию
ls -ld database/

# Должно быть drwxrwxrwx или drwxr-xr-x
# Если нет - исправьте:
chmod 777 database
```

### Шаг 2: Проверка переменной DATABASE_URL

```bash
# Проверьте переменную внутри контейнера
docker-compose exec web env | grep DATABASE_URL

# Должно быть: DATABASE_URL=file:./database/db.sqlite
```

### Шаг 3: Создание базы данных вручную

```bash
# Попробуйте создать файл вручную для проверки прав
touch database/db.sqlite
chmod 666 database/db.sqlite

# Проверьте, что файл создан
ls -la database/db.sqlite
```

### Шаг 4: Выполнение миграций с проверкой прав

```bash
# Установите правильные права
chmod 777 database

# Выполните миграции снова
docker-compose exec web npm run db:migrate

# Проверьте создание файла
ls -la database/db.sqlite
```

### Шаг 5: Если файл все еще не создается

```bash
# Проверьте логи Prisma
docker-compose logs web | grep -i "prisma\|database\|sqlite"

# Попробуйте создать базу данных напрямую через Prisma
docker-compose exec web npx prisma db push

# Или через миграции с verbose
docker-compose exec web npm run db:migrate -- --verbose
```

### Шаг 6: Альтернативное решение - создание через Prisma Studio

```bash
# Запустите Prisma Studio (откроется на порту 5555)
docker-compose exec web npm run db:studio

# Или создайте базу данных через Prisma напрямую
docker-compose exec web npx prisma db push --accept-data-loss
```

## 🔍 Диагностика

### Проверка прав внутри контейнера

```bash
# Проверьте права на директорию внутри контейнера
docker-compose exec web ls -ld /app/database

# Проверьте пользователя контейнера
docker-compose exec web whoami
docker-compose exec web id

# Попробуйте создать файл внутри контейнера
docker-compose exec web touch /app/database/test.txt
docker-compose exec web ls -la /app/database/
```

### Проверка volume mount

```bash
# Проверьте, что volume правильно смонтирован
docker-compose exec web mount | grep database

# Проверьте содержимое директории
docker-compose exec web ls -la /app/database/
```

### Проверка переменных окружения

```bash
# Проверьте все переменные окружения
docker-compose exec web env | grep -E "DATABASE|NODE"

# Проверьте .env файл на хосте
cat .env | grep DATABASE_URL
```

## 🛠️ Полное исправление

Если ничего не помогает, выполните полное исправление:

```bash
cd /var/www/zelyonkin.ru

# 1. Остановите контейнеры
docker-compose down

# 2. Удалите старую директорию database (ОСТОРОЖНО: удалит данные!)
rm -rf database/*

# 3. Создайте директорию с правильными правами
mkdir -p database
chmod 777 database

# 4. Создайте пустой файл базы данных
touch database/db.sqlite
chmod 666 database/db.sqlite

# 5. Запустите контейнер
docker-compose up -d web

# 6. Подождите 10 секунд
sleep 10

# 7. Выполните миграции
docker-compose exec web npm run db:migrate

# 8. Проверьте создание
ls -la database/db.sqlite

# 9. Создайте администратора
docker-compose exec web npm run db:force-fix-user
```

## 📝 Примечания

- **Права 777** могут быть необходимы для volume mount в Docker
- **Пользователь контейнера** (nextjs, UID 1001) должен иметь права на запись
- **Volume mount** должен правильно синхронизировать файлы между контейнером и хостом
- **DATABASE_URL** должен указывать на правильный путь: `file:./database/db.sqlite`

---

**Версия:** 2.2.1  
**Дата:** 2025-01-18

