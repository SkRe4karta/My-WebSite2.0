# Инструкция по обновлению на ВМ

## Версия 2.2.1 - Обновление аутентификации и управления пользователями

### Дата: 2025-01-18

## 🎯 Что изменилось

### 1. Исправлена аутентификация
- ✅ Убрана привязка к одному пользователю (ADMIN_USERNAME)
- ✅ Поддержка входа по email или name для всех пользователей
- ✅ Улучшена конфигурация NextAuth (убрано PrismaAdapter при JWT strategy)
- ✅ Исправлена работа сессий

### 2. Добавлено управление пользователями
- ✅ API endpoints для CRUD операций с пользователями
- ✅ UI компонент UserManagement в настройках
- ✅ Возможность создавать, редактировать и удалять пользователей
- ✅ Управление паролями и ролями пользователей

### 3. Исправлены миграции базы данных
- ✅ Изменен `db:migrate` на `prisma migrate deploy` для продакшена
- ✅ Улучшена обработка ошибок в скриптах установки

### 4. Добавлены диагностические скрипты
- ✅ `npm run db:test-login` - тестирование входа
- ✅ `npm run db:check-auth` - проверка настроек аутентификации
- ✅ `npm run db:fix-user` - исправление пользователя
- ✅ `npm run db:force-fix-user` - принудительное исправление пользователя

### 5. Исправлена инициализация администратора
- ✅ Автоматическое создание администратора после миграций
- ✅ Правильное сохранение name пользователя (skre4karta)
- ✅ Автоматическая генерация bcrypt хеша пароля

## 📋 Новые файлы

```
scripts/
├── init-admin.js          # Создание администратора
├── check-auth.js          # Проверка настроек аутентификации
├── fix-user.js            # Исправление пользователя
├── force-fix-user.js     # Принудительное исправление
└── test-login.js          # Тестирование входа

src/
├── app/api/users/
│   ├── route.ts          # GET, POST - список и создание пользователей
│   └── [id]/route.ts     # PUT, DELETE - обновление и удаление
└── components/admin/
    └── UserManagement.tsx # UI управления пользователями
```

## 🚀 Инструкция по обновлению на ВМ

### Шаг 1: Подготовка на локальной машине

```bash
cd my-portfolio-site

# Проверьте изменения
git status

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "feat: добавлено управление пользователями и исправлена аутентификация

- Исправлена аутентификация NextAuth (поддержка множественных пользователей)
- Добавлено управление пользователями в настройках
- Исправлены миграции базы данных
- Добавлены диагностические скрипты
- Автоматическое создание администратора при установке"

# Отправьте в репозиторий
git push origin main
```

### Шаг 2: Обновление на ВМ

```bash
# Подключитесь к серверу
ssh admin@82.202.138.157

# Перейдите в директорию проекта
cd /var/www/zelyonkin.ru

# Остановите контейнеры
docker-compose down

# Получите обновления из git
git pull origin main

# Пересоберите образы
docker-compose build --no-cache

# Запустите контейнеры
docker-compose up -d

# Дождитесь запуска
sleep 10

# Примените миграции (если есть новые)
docker-compose exec web npm run db:migrate

# Исправьте пользователя (если нужно)
docker-compose exec web npm run db:force-fix-user

# Проверьте статус
docker-compose ps
docker-compose logs web
```

### Шаг 3: Проверка работы

```bash
# Проверьте настройки аутентификации
docker-compose exec web npm run db:check-auth

# Протестируйте вход
docker-compose exec web npm run db:test-login

# Проверьте healthcheck
curl http://localhost/api/health
```

## 🔧 Исправление проблем с входом

Если после обновления не работает вход:

```bash
# 1. Исправьте пользователя
docker-compose exec web npm run db:force-fix-user

# 2. Проверьте результат
docker-compose exec web npm run db:test-login

# 3. Проверьте логи при попытке входа
docker-compose logs -f web
```

## 📝 Учетные данные по умолчанию

- **Логин:** `skre4karta` или `zelyonkin.d@gmail.com`
- **Пароль:** `1234`

⚠️ **ВАЖНО:** После первого входа обязательно смените пароль в настройках!

## 🆕 Новые возможности

### Управление пользователями

1. Войдите в админку
2. Перейдите в **Настройки**
3. В правой колонке найдите **"Управление пользователями"**
4. Используйте кнопку **"+ Добавить пользователя"** для создания новых пользователей
5. Редактируйте и удаляйте пользователей через соответствующие кнопки

### Диагностические команды

```bash
# Проверка настроек аутентификации
npm run db:check-auth

# Тестирование входа
npm run db:test-login

# Исправление пользователя
npm run db:fix-user

# Принудительное исправление (генерирует новый хеш)
npm run db:force-fix-user
```

## 📚 Дополнительная документация

- `DIAGNOSE-LOGIN.md` - подробная диагностика проблем с входом
- `CHANGELOG.md` - история всех изменений
- `README.md` - общая документация проекта

## ⚠️ Важные замечания

1. **Миграции:** Теперь используется `prisma migrate deploy` вместо `prisma migrate dev` для продакшена
2. **Пользователи:** Можно входить по email или name (логину)
3. **Пароли:** Всегда хранятся в bcrypt формате
4. **Администратор:** Создается автоматически при установке с данными из `.env`

## 🔄 Откат изменений (если нужно)

```bash
# На сервере
cd /var/www/zelyonkin.ru
git log --oneline -10  # Найдите предыдущий коммит
git checkout <commit-hash>  # Откатитесь к предыдущей версии
docker-compose build --no-cache
docker-compose up -d
```

